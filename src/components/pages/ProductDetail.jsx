import { useParams } from "react-router-dom"
import MainLayout from "../MainLayout"

const ProductDetail = () => {
    const {id} = useParams();
  return (
    <MainLayout>
      <div>
        
      </div>
    </MainLayout>
  )
}

export default ProductDetail
